{
  "request": {"argv": ["tn", "kg", "query", "--token", "tok-analyst-binh", "MATCH (c:Concept) WHERE c.name =~ '(?i).*basket.*' OR any(a IN c.aliases WHERE a =~ '(?i).*basket.*') OPTIONAL MATCH (c)<-[:VARIANT_OF]-(v:Concept)-[:MEASURED_BY]->(m:Metric) RETURN c, v, m"]},
  "response": {
    "contract_version": "0.3",
    "ok": true,
    "user": {"id": "u_binh", "role": "data_analyst"},
    "result": {
      "records": [
        {
          "c": {"_label": "Concept", "key": "basket_size", "name": "Basket size", "definition": "How big a basket is. Ambiguous: item count vs monetary value. B2B baskets are few but huge in value, so the two tell different stories.", "aliases": ["basket size", "average basket", "order size", "cart size"]},
          "v": {"_label": "Concept", "key": "basket_by_items", "name": "Basket size by items", "definition": "Average number of items per basket.", "aliases": ["items per basket", "units per order"]},
          "m": {"_label": "Metric", "key": "basket_items_avg", "name": "Average basket size (items)", "description": "Mean item count per basket.", "type": "ratio", "unit": "count", "formula": "sum(qty) / count(distinct basket_id)", "version": "0.1", "_access": {"readable": true}}
        },
        {
          "c": {"_label": "Concept", "key": "basket_size", "name": "Basket size", "definition": "How big a basket is. Ambiguous: item count vs monetary value. B2B baskets are few but huge in value, so the two tell different stories.", "aliases": ["basket size", "average basket", "order size", "cart size"]},
          "v": {"_label": "Concept", "key": "basket_by_value", "name": "Basket size by value", "definition": "Average monetary value per basket.", "aliases": ["value per basket", "average order value", "aov"]},
          "m": {"_label": "Metric", "key": "basket_value_avg", "name": "Average basket size (value)", "description": "Mean net value per basket.", "type": "ratio", "unit": "VND", "formula": "sum(net_amount) / count(distinct basket_id)", "version": "0.1", "_access": {"readable": true}}
        }
      ]
    },
    "metadata": {
      "graph_compiled_at": "2026-07-11T18:00:00",
      "applied_permissions": [
        {"type": "column_tokenized", "column": "fact_sales_lines.customer_id", "display": "customer ids are stable tokens"}
      ]
    },
    "warnings": [],
    "error": null
  }
}
