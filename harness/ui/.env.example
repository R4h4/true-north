# Next.js proxy -> AG-UI backend. Default http://localhost:8000/ works for
# local dev; point at the FastAPI service URL when deployed.
# AGUI_URL=http://localhost:8000/

# CopilotKit runtime phones home anonymous telemetry unless disabled.
COPILOTKIT_TELEMETRY_DISABLED=true
